{
  "scripts": {
    "serve": "tsc --watch",
    "build": "npx esbuild assets/scripts/main.ts --bundle --minify --outfile=dist/main.js --format=iife --target=es2018"
  },
  "devDependencies": {
    "terser": "^5.44.0",
    "typescript": "^5.6.3"
  }
}
